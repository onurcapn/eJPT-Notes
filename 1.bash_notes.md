# Linux CLI & Temel Bash Notları

## 📋 Giriş ve Temel Araçlar
* **Dirb**: Web dizin tarama/keşif aracı.
* **Nc (Netcat)**: Ağ bağlantıları okuma/yazma (port tarama, shell alma vb.) için "İsviçre çakısı".
* **Man**: Komut kılavuz sayfaları (örn: `man ls`).

---

## 🐧 Linux CLI Temelleri (TryHackMe - Linux CLI Basics)
TryHackMe üzerindeki Linux CLI Basics (Temel Linux Komut Satırı) odası, Linux işletim sisteminde terminal kullanımının ve temel komutların mantığını öğretmeyi hedefler.

### 1. Dizin Gezinmesi ve Konum Bilgisi (Navigation)
Linux'ta grafik arayüz (GUI) yerine klasörler arasında terminalden gezinirken en sık kullanılan komutlar:

* **`pwd` (Print Working Directory)**: O anda hangi dizinde (klasörde) bulunduğunuzu gösterir.
  ```bash
  pwd
  # Çıktı örneği: /home/user
  ```
* **`ls` (List)**: Bulunduğunuz dizindeki dosya ve klasörleri listeler.
  * `ls -l` : Detaylı liste görünümü (izinler, dosya boyutu, sahibi vb.).
  * `ls -a` : Gizli dosyaları da gösterir (nokta `.` ile başlayan dosyalar).
  * `ls -la` : Hem detaylı hem gizli dosyaları listeler.
* **`cd` (Change Directory)**: Dizin değiştirmek için kullanılır.
  * `cd /var/www` : Belirtilen yola gider.
  * `cd ..` : Bir üst dizine çıkar.
  * `cd ~` veya sadece `cd` : Mevcut kullanıcının ev (home) dizinine döner.

---

### 2. Dosya ve Dizin Yönetimi
Dosya ve klasör oluşturma, kopyalama, taşıma ve silme işlemleri:

* **`mkdir` (Make Directory)**: Yeni bir klasör oluşturur.
  ```bash
  mkdir yeni_klasor
  ```
* **`touch`**: Boş bir dosya oluşturur veya var olan bir dosyanın erişim zamanını günceller.
  ```bash
  touch notlar.txt
  ```
* **`cp` (Copy)**: Dosya veya klasör kopyalar.
  * `cp dosya.txt /tmp/` : Dosyayı belirtilen dizine kopyalar.
  * `cp -r klasor1/ klasor2/` : Klasörü içindekilerle birlikte kopyalar (`-r` recursive).
* **`mv` (Move)**: Dosya/klasör taşır veya adını değiştirir.
  * `mv eski_ad.txt yeni_ad.txt` : Dosya adını değiştirir.
  * `mv dosya.txt /tmp/` : Dosyayı taşır.
* **`rm` (Remove)**: Dosya veya klasör siler.
  * `rm dosya.txt` : Dosya siler.
  * > [!WARNING]
    > `rm -rf klasor/` : Klasörü ve içindekileri kalıcı olarak siler (`-r` recursive, `-f` force). Geri dönüşü yoktur!

---

### 3. Dosya İçeriğini Okuma ve Düzenleme
Metin dosyalarının içeriğini görüntülemek ve düzenlemek için kullanılan temel araçlar:

* **`cat` (Concatenate)**: Dosya içeriğini ekrana yazdırır.
  ```bash
  cat /etc/passwd
  ```
* **`less` / `more`**: Büyük dosyaları sayfa sayfa okumanızı sağlar (`Q` tuşu ile çıkılır).
* **`head` ve `tail`**: Dosyanın başını veya sonunu gösterir.
  * `head -n 5 dosya.txt` : İlk 5 satırı gösterir.
  * `tail -n 10 dosya.txt` : Son 10 satırı gösterir.
  * `tail -f /var/log/syslog` : Log dosyasını canlı olarak takip etmeyi sağlar.
* **`nano`**: Terminal tabanlı, kullanımı kolay bir metin düzenleyicidir.
  ```bash
  nano notlar.txt
  ```

---

### 4. Arama ve Filtreleme Komutları
Sistemde belirli metinleri veya dosyaları bulmak için kritik öneme sahiptir:

* **`grep`**: Dosya içinde veya çıktıda belirli bir metin kalıbını arar.
  ```bash
  grep "root" /etc/passwd
  ```
* **`find`**: Dizinler içinde dosya veya klasör arar.
  * `find / -name "config.php" 2>/dev/null` : Kök dizinden itibaren ismi `config.php` olan dosyayı arar.
* **`wc` (Word Count)**: Satır, kelime veya karakter sayısını verir.
  * `wc -l dosya.txt` : Dosyadaki toplam satır sayısını gösterir.

---

### 5. Yönlendirme (Redirection) ve Borulama (Piping)
Komutların çıktılarını başka komutlara veya dosyalara yönlendirme mantığıdır:

* **`>` (Yeniden Yönlendirme - Üzerine Yazma)**: Komutun çıktısını bir dosyaya yazar (varsa eski içeriği siler).
  ```bash
  echo "Merhaba Linux" > mesaj.txt
  ```
* **`>>` (Ekleme)**: Çıktıyı var olan dosyanın sonuna ekler.
  ```bash
  echo "İkinci Satır" >> mesaj.txt
  ```
* **`|` (Pipe - Boru)**: Bir komutun çıktısını, arkasından gelen komuta girdi olarak aktarır.
  ```bash
  cat /etc/passwd | grep "bash"
  ```

---

### 6. Kullanıcılar, Yetkiler ve İzinler (Permissions)
Linux'ta her dosya ve klasörün erişim yetkileri vardır.

* **`whoami`**: Şu an hangi kullanıcı olduğunuzu gösterir.
* **`id`**: Kullanıcının UID (User ID) ve GID (Group ID) bilgilerini gösterir.
* **`chmod` (Change Mode)**: Dosya izinlerini değiştirir.
  ```bash
  chmod 755 betik.sh    # Sahibi: rwx, Grubu: r-x, Diğerleri: r-x
  chmod +x betik.sh     # Dosyaya çalıştırma yetkisi ekler
  ```
* **`chown` (Change Owner)**: Dosyanın sahibini veya grubunu değiştirir.
  ```bash
  sudo chown user:group dosya.txt
  ```
* **`sudo` (SuperUser Do)**: Komutları yönetici (root) yetkisiyle çalıştırmayı sağlar.

---

#### 🔒 Linux İzin Yapısı ve Mantığı
Linux'ta izinler 3 farklı gruba dağıtılır:
* **Sahip (User - `u`)**
* **Grup (Group - `g`)**
* **Diğerleri (Others - `o`)**

Her grupta 3 yetki bulunur:
* **Okuma (Read - `r`)**: `4`
* **Yazma (Write - `w`)**: `2`
* **Çalıştırma (Execute - `x`)**: `1`

---

#### `chmod 755` vs `chmod +x` Karşılaştırması

##### 1. `chmod 755 betik.sh` (Kesin/Mutlak Tanımlama)
Tüm grupların izinlerini rakamlar kullanarak tek seferde sıfırdan belirler:
* **7 (Sahibi)**: `4 + 2 + 1 = rwx` (Okur, Yazar, Çalıştırır)
* **5 (Grubu)**: `4 + 0 + 1 = r-x` (Okur, Çalıştırır)
* **5 (Diğerleri)**: `4 + 0 + 1 = r-x` (Okur, Çalıştırır)

> [!NOTE]
> Dosyanın eski izinleri ne olursa olsun, hepsini siler ve tam olarak bu sayısal karşılığı atar.

##### 2. `chmod +x betik.sh` (Eklemeli/Relatif Tanımlama)
Var olan izinlere dokunmadan, sadece çalıştırma (`x`) yetkisini ekler:
* Başına harf koymadığınız için (örn: `u`, `g` veya `o` demediğiniz için) bu çalıştırma yetkisini herkese (Sahip, Grup, Diğerleri) ilave eder.
* Dosyanın mevcut okuma/yazma izinleri neyse olduğu gibi kalır, sadece üzerine `+x` eklenir.

**Özetle:**
* **`755`**: "Tüm izin yapısını yeniden ayarla: Sahibe tam yetki ver, gruba ve diğerlerine sadece okuma/çalıştırma ver."
* **`+x`**: "Mevcut izinlere dokunma, sadece dosyayı çalıştırılabilir hale getir."

---

#### ✍️ Sembolik (Harfli) İzin Yönetimi
Linux'ta izinleri kullanıcı türlerine göre özelleştirmek için harfli (sembolik) yöntemi kullanırız.

**Temel Kullanıcı Türleri:**
* **`u` (User)**: Dosyanın sahibi
* **`g` (Group)**: Dosyanın grubu
* **`o` (Others)**: Diğer herkes
* **`a` (All)**: Herkes (Sahip + Grup + Diğerleri)

##### En Çok Kullanılan Yöntemler

1. **Her Kategoriye Farklı Yetki Vermek (Tek Komutta)**
   Virgülle ayırarak herkese farklı bir yetki tanımlayabilirsiniz:
   ```bash
   chmod u=rwx,g=rx,o=r betik.sh
   ```
   * **Sahibi (`u`)**: Okur, yazar, çalıştırır (`rwx`)
   * **Grubu (`g`)**: Okur, çalıştırır (`rx`)
   * **Diğerleri (`o`)**: Sadece okur (`r`)

2. **Sadece Belirli Bir Gruba İzin Eklemek/Çıkarmak**
   Var olan izinleri bozmadan sadece tek bir gruba müdahale edebilirsiniz:
   * `chmod g+w betik.sh` ➔ Sadece gruba yazma (`w`) yetkisi ekler.
   * `chmod o-r betik.sh` ➔ Diğer herkesten okuma (`r`) yetkisini kaldırır.
   * `chmod u+x betik.sh` ➔ Sadece sahibine çalıştırma (`x`) yetkisi verir.

3. **Sayısal (Oktal) Yöntemle Değiştirmek**
   Herkesin yetkisini tek seferde sıfırdan ayarlamanın en hızlı yolu 3 haneli sayı kullanmaktır:
   ```bash
   chmod 740 betik.sh
   ```
   * **1. Rakam (7)**: Sahibi ➔ `4 + 2 + 1 = rwx` (Tam Yetki)
   * **2. Rakam (4)**: Grubu ➔ `4` (Sadece Okuma)
   * **3. Rakam (0)**: Diğerleri ➔ `0` (Hiçbir Yetki Yok)

> [!TIP]
> **Kısaca Mantık:** Harfle yapacaksanız `Kime (u/g/o/a)` + `İşlem (+/-/=)` + `Yetki (r/w/x)` formülünü kullanırsınız.

---

### 7. Süreç (Process) Yönetimi
Arka planda veya ön planda çalışan uygulamaları kontrol etme:

* **`ps` (Process Status)**: Anlık çalışan süreçleri gösterir.
  ```bash
  ps aux
  ```
* **`top` / `htop`**: Sistem kaynaklarını ve çalışan süreçleri canlı olarak izler.
* **`kill`**: Belirli bir süreci ID numarasına (PID) göre sonlandırır.
  ```bash
  kill -9 <PID>
  ```
