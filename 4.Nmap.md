# 🛡️ Nmap Notları & Cheat Sheet 🚀

## 📋 Temel Seçenekler (Switches)

| Anahtar | Açıklama |
| :--- | :--- |
| **`-sS`** | SYN Taraması (Varsayılan yetkili tarama) |
| **`-sU`** | UDP Taraması |
| **`-sn`** | Ping Taraması (Port taraması yapmadan sadece aktif cihazları tespit eder) |
| **`-O`** | İşletim Sistemi (OS) Tespiti |
| **`-sV`** | Versiyon/Hizmet Sürümü Tespiti |
| **`-v` / `-vv`** | Detay Düzeyi (Verbosity) Artırma |
| **`-oA`** | Sonuçları 3 ana formatta da kaydeder (Normal, XML, Grepable) |
| **`-oN`** | Sonuçları normal metin formatında kaydeder |
| **`-oG`** | Sonuçları "grep ile aranabilir" formatta kaydeder |
| **`-A`** | Agresif Tarama (OS, Versiyon, Script, Traceroute aktif eder) |
| **`-T0` - `-T5`** | Tarama Hızı / Zamanlama Şablonu (`-T5` en hızlı, en gürültülü) |
| **`-p 80`** | Sadece port 80'i tarar |
| **`-p 1000-1500`** | Belirli bir port aralığını tarar |
| **`-p-`** | Tüm portları (1-65535) tarar |
| **`-sC`** | Varsayılan NSE betiklerini (Default Scripts) çalıştırır |
| **`--script`** | NSE Script çalıştırmayı aktif eder |
| **`--script=vuln`** | "Vuln" kategorisindeki tüm güvenlik açığı tarama komut dosyalarını çalıştırır |
| **`-Pn`** | Taramadan önce ping atmayı atlar (ICMP engelli sistemler için) |
| **`-f`** | Paketleri küçük parçalara (fragment) böler (Güvenlik duvarı/IDS atlatma) |
| **`--mtu <değer>`** | Özel MTU boyutu belirler (8'in katı olmalıdır) |
| **`--scan-delay <zaman>ms`** | Paket gönderimleri arasına gecikme ekler |
| **`--badsum`** | Geçersiz sağlama toplamına (checksum) sahip paketler yollar (Güvenlik duvarı tespiti) |

---

## ⚙️ Temel TCP Bayrakları (Flags) ve Anlamları
TCP el sıkışmasında ve taramalarda paketlerin durumunu belirtmek için kullanılan temel bayraklar:

* 📡 **SYN (Synchronize)**: **Bağlantı Başlatma.** İki cihaz arasında yeni bir TCP bağlantısı (iletişim) başlatmak için gönderilen ilk istek paketidir.
* ✅ **ACK (Acknowledgment)**: **Onay/Alındı.** Karşı taraftan gelen bir paketin başarıyla ulaştığını doğrulamak ve onaylamak için gönderilir.
* 🛑 **RST (Reset)**: **Bağlantıyı Sıfırla.** Bağlantıyı anında ve zorla kapatmak için kullanılır. Genellikle kapalı bir porta istek atıldığında sunucu "bağlanamazsın" diyerek RST döner.
* 🏁 **FIN (Finish)**: **Sonlandırma.** Veri aktarımı bittikten sonra bağlantıyı karşılıklı ve güvenli bir şekilde kapatmak için kullanılır.

---

## 🚦 Temel Tarama Türleri ve Mantığı

### 🚀 Nmap Temel Tarama Türleri Detayları

#### 1. TCP Connect Taraması (`-sT`)
* **Protokol:** TCP
* **Mantık:** Tam el sıkışma (`SYN` ➔ `SYN-ACK` ➔ `ACK`) yapar.
* **Özelliği:** Sıradan bir kullanıcı gibi hedefe tam bağlanır. `sudo` yetkisi gerektirmez.
* **Log Durumu:** 🔴 **Yüksek (Uygulama Seviyesi).** Tam bağlantı kurulduğu için web sunucusu, SSH veya işletim sistemi loglarına *"X IP'si bağlandı"* şeklinde direkt kaydedilir.

#### 2. TCP SYN Taraması (`-sS`)
* **Protokol:** TCP
* **Mantık:** Yarı açık el sıkışma (`SYN` ➔ `SYN-ACK` ➔ `RST`) yapar.
* **Özelliği:** Tam el sıkışma bitmeden `RST` atıp kaçtığı için hızlıdır. Nmap'in varsayılan modudur (Linux'ta ham paket oluşturmak için `sudo` ister).
* **Log Durumu:** 🟡 **Düşük (Stealth / Gizli).** Bağlantı tamamlanmadığı için servisler/uygulamalar log tutmaz. Sadece gelişmiş ağ güvenlik sistemleri (IDS/IPS) fark edebilir.

#### 3. UDP Taraması (`-sU`)
* **Protokol:** UDP
* **Mantık:** Paketi fırlatıp sessizliği veya ICMP yanıtını dinler.
* **Özelliği:** UDP bağlantısız olduğu için yanıt gelmezse portu `open|filtered` kabul eder. Paket kayıpları ve tekrar denemeler yüzünden en yavaş tarama türüdür.
* **Log Durumu:** 🔴 **Çok Yüksek (Ağ Seviyesi).** Kapalı portlardan dönen ICMP paketleri ağda aşırı trafik ve gürültü yaratır. Güvenlik duvarları (Firewall) ve IDS sistemleri bu yoğunluğu anında tespit edip alarm üretir.

> [!TIP]
> **Özet:** Protokol mantıkları ve ağ güvenlik sistemlerine (SOC/SIEM) nasıl yakalandıkları bu notlarla netleşmiş durumdadır.

---

### 🔍 Taramalar Arasındaki Temel Farklar

#### 1. Paket Yapısı ve Bayraklar (Flags)
* **Standart TCP Taramaları (`-sT`, `-sS`):** Kuralına uygun (RFC standartlarına sadık) paketler gönderir. Bir iletişim başlatmak için `SYN` bayrağını kullanır.
* **Stealth Taramalar (`-sN`, `-sF`, `-sX`):** Kural dışı, kasten hatalı/anormal paketler gönderir.
    * `NULL`: Hiçbir bayrak yok (boş paket).
    * `FIN`: Sadece bağlantı kapatma bayrağı var (ortada açık bir bağlantı yokken).
    * `Xmas`: `FIN`, `PSH` ve `URG` bayrakları aynı anda yanan karmaşık paket.
* **UDP Taraması (`-sU`):** TCP protokolünü hiç kullanmaz, doğrudan UDP veri paketleri fırlatır.

#### 2. Güvenlik Duvarı (Firewall) Atlatma Mantığı
* **SYN Taraması (`-sS`):** Basit güvenlik duvarları `SYN` bayrağı taşıyan yeni bağlantı isteklerini hemen fark eder ve engeller.
* **NULL / FIN / Xmas:** Paketlerin içinde `SYN` bayrağı bulunmadığı için, sadece `SYN` paketlerini filtresine almış eski/zayıf güvenlik duvarı kurallarının yanından sıyrılıp içeri sızabilir.
* **UDP:** Port durumunu TCP kurallarıyla tespit edemediği için güvenlik duvarları arkasındaki UDP servislerini bulmak bambaşkadır.
* **Ping Engeli Atlatma (`-Pn`):** Birçok güvenlik duvarı/sistem varsayılan olarak ICMP (ping) paketlerini engeller. Nmap normalde taramaya başlamadan önce hedefin aktif olup olmadığını anlamak için ping atar; yanıt alamazsa taramayı sonlandırır. `-Pn` parametresi, hedefin her halükarda aktif olduğunu kabul ederek ping atmayı atlar ve doğrudan port taramasına geçer.

#### 3. İşletim Sistemi Bağımlılığı ve Doğruluk (Kritik Fark!)
* **Standart Taramalar (`-sT`, `-sS`):** Tüm işletim sistemlerinde (Linux, Windows, macOS, Cisco vb.) %100 standart ve doğru çalışır.
* **NULL / FIN / Xmas:** İşletim sistemine göre davranışı değişir!
    * **Linux / Unix / BSD:** RFC 793 standartlarına uyar. Port kapalıysa `RST` yanıtı verir, açık veya filtreliyse yanıt vermez (`open|filtered`).
    * **Windows / Bazı Cisco Cihazlar:** Standarta uymaz! Port ister açık ister kapalı olsun bu anormal paketlerin hepsine `RST` (Kapalı) yanıtı döner. Bu yüzden Windows hedeflerde bu 3 tarama türü bütün portları yanlışlıkla kapalı gösterir.

---

### 📊 Tarama Türleri Karşılaştırma Tablosu

| Özellik / Tarama Türü | TCP Connect (`-sT`) | TCP SYN (`-sS`) | NULL / FIN / Xmas | UDP (`-sU`) |
| :--- | :--- | :--- | :--- | :--- |
| **Kullanılan Bayraklar** | `SYN` ➔ `ACK` | `SYN` ➔ `RST` | Hiçbiri / `FIN` / `FIN-PSH-URG` | Bayrak Yok (UDP) |
| **Ana Amacı** | Standart Bağlantı | Hızlı & Temel Stealth | Firewall Atlatma (Evasion) | UDP Servis Tespiti |
| **Gerekli Yetki** | Normal Kullanıcı | Root / Sudo | Root / Sudo | Root / Sudo |
| **Windows Uyumluluğu** | ✅ Mükemmel | ✅ Mükemmel | ❌ Çalışmaz (Hep kapalı der) | ✅ Çalışır |
| **Açık Port Yanıtı** | `SYN/ACK` | `SYN/ACK` | Yanıt Yok (`open|filtered`) | Yanıt Yok (`open|filtered`) |
| **Kapalı Port Yanıtı** | `RST` | `RST` | `RST` | `ICMP Port Unreachable` |

---

### 💡 Kısa Özet
* **SYN (`-sS`):** Günlük hayatta ve pentest'lerde varsayılan ve en güvenilir tercihtir.
* **NULL/FIN/Xmas:** Sadece "Güvenlik duvarında SYN engeli var mı, arkasına paket geçirebiliyor muyum?" diye test etmek veya Linux hedefli özel sızma durumlarında kullanılır.
* **UDP (`-sU`):** Yalnızca DNS, DHCP, SNMP gibi UDP tabanlı servisleri ararken başvurulan ayrı bir kategoridir.

---

## 🛰️ Ağ Keşfi & Ping Taraması (Host Discovery)
Ağdaki aktif hostların tespiti:
```bash
nmap -sn 192.168.0.1-254
# veya CIDR gösterimi ile:
nmap -sn 192.168.0.0/24
```
* **`-sn`**: Port taraması yapmadan sadece hostların açık olup olmadığını belirlemek için ping taraması gerçekleştirir.

---

## 🚫 Ping Engeli ve `-Pn` (No-Ping) Parametresi

### ❓ Neden Kullanılır?
* Varsayılan olarak Nmap, tarama öncesi hedefin aktifliğini kontrol etmek için **ICMP (ping)** paketi gönderir.
* Hedef yanıt vermezse tarama durdurulur. Ancak modern sistemler (örn. Windows Firewall) güvenlik amacıyla **ping paketlerini engeller/düşürür**.

### 🛠️ Çözüm ve Çalışma Mantığı
* **`-Pn`** parametresi, tarama öncesindeki ping denetlemesini **tamamen atlar**.
* Nmap, hedefin **aktif olduğunu kabul ederek** doğrudan port taramasına başlar.

> [!WARNING]
> **Dezavantajı:** Hedef gerçekten kapalıysa, Nmap bunu ping ile doğrulayamadığı için tüm portları tek tek sorgular ve tarama süresi ciddi şekilde uzar.

### 🔗 Hangi Tarama Türleriyle Birlikte Kullanılır?
`-Pn` bağımsız bir keşif değiştiricisidir (modifier). Port tarama tekniğini belirlemez, sadece tarama öncesi ping kontrolünü kapatır. Bu yüzden **tüm tarama türleriyle birlikte kombine edilebilir**:

* ⚡ **TCP SYN Tarama ile (`-sS -Pn`):** `sudo nmap -sS -Pn <hedef>` *(Gerçek hayatta ve CTF'lerde en sık kullanılan ikilidir; en hızlı/gizli TCP tarama yöntemi ile ping engeli atlatma özelliğini birleştirerek en kararlı sonucu verir).*
* 🔌 **TCP Connect Tarama ile (`-sT -Pn`):** `nmap -sT -Pn <hedef>` *(Sudo/root yetkiniz olmadığında tercih edilir).*
* 🔋 **UDP Tarama ile (`-sU -Pn`):** `sudo nmap -sU -Pn <hedef>` *(UDP portlarını tararken ping engeli varsa kullanılır).*
* 👻 **Stealth Taramalar ile (`-sX -Pn` / `-sN -Pn`):** `sudo nmap -sX -Pn <hedef>` *(Güvenlik duvarı kurallarını atlatmak için Xmas/NULL/FIN taramasıyla birleştirilir).*
* 🔍 **Servis ve OS Tespiti ile (`-sV -O -Pn`):** `sudo nmap -sS -sV -O -Pn <hedef>` *(Derinlemesine taramalarda ping engeli varsa kullanılır).*

---

## 🐍 Nmap Scripting Engine (NSE)
Lua dilinde yazılmış ek betik kütüphanesidir. `/usr/share/nmap/scripts/` dizininde saklanır.

* **Kategoriler:** `safe`, `intrusive`, `vuln`, `exploit`, `auth`, `brute`, `discovery`
* **Varsayılan Tarama (`-sC`):** Nmap'in varsayılan/temel betik kategorisini (equivalent to `--script=default`) çalıştırır. Sınavda en sık kullanılan parametrelerden biridir; hedef servisler hakkında hızlı ve güvenli ek bilgi toplar.
* **`script.db` NSE Arama Yöntemleri:**
  ```bash
  grep "ftp" /usr/share/nmap/scripts/script.db
  # veya:
  ls -l /usr/share/nmap/scripts/*ftp*
  ```
* **Örnek Komut Dosyası ve Argüman Kullanımı:**
  ```bash
  nmap -p 80 --script http-put --script-args http-put.url='/dav/shell.php',http-put.file='./shell.php' <hedef>
  ```
* **NSE Veritabanını Güncelleme:**
  ```bash
  sudo nmap --script-updatedb
  ```

---

## 📝 Pratik Sorular & Çözüm Adımları (Görev 14)

### 1. Soru
* **Soru:** Hedef IP adresi ICMP yankı (ping) isteklerine yanıt veriyor mu?
* **Çözüm:** `ping -c 3 10.112.132.222` komutu atıldığında %100 paket kaybı yaşanır.
* **Cevap:** `H` *(veya ingilizce arayüzde N)*

### 2. Soru
* **Soru:** Hedef cihazın ilk 999 portunda Noel (Xmas) taraması gerçekleştirin; kaç portun açık veya filtrelenmiş (open|filtered) olduğu görülüyor?
* **Çözüm:** `sudo nmap -sX -p 1-999 -Pn 10.112.132.222`
* **Cevap:** `999`

### 3. Soru
* **Soru:** Bunun bir sebebi var, peki o sebep nedir?
* **Cevap:** `No Response` *(Güvenlik duvarı paketleri düşürür ve yanıt dönmez)*

### 4. Soru
* **Soru:** Hedefteki ilk 5000 port üzerinde TCP SYN taraması gerçekleştirin; kaç portun açık olduğu görülüyor?
* **Çözüm:** `sudo nmap -sS -p 1-5000 -Pn 10.112.132.222`
* **Cevap:** `5`

### 5. Soru
* **Soru:** Nmap, 21 numaralı porttaki FTP sunucusuna ftp-anon komut dosyası ile başarıyla giriş yapabiliyor mu?
* **Çözüm:** `nmap -p 21 --script=ftp-anon -Pn 10.112.132.222`
* **Cevap:** `E` *(veya ingilizce arayüzde Y)*

---

### 🔍 Taramalar Arasındaki Temel Farklar (Kopya Not)

#### 1. Paket Yapısı ve Bayraklar (Flags)
* **Standart TCP Taramaları (`-sT`, `-sS`):** Kuralına uygun (RFC standartlarına sadık) paketler gönderir. Bir iletişim başlatmak için `SYN` bayrağını kullanır.
* **Stealth Taramalar (`-sN`, `-sF`, `-sX`):** Kural dışı, kasten hatalı/anormal paketler gönderir.
    * `NULL`: Hiçbir bayrak yok (boş paket).
    * `FIN`: Sadece bağlantı kapatma bayrağı var (ortada açık bir bağlantı yokken).
    * `Xmas`: `FIN`, `PSH` ve `URG` bayrakları aynı anda yanan karmaşık paket.
* **UDP Taraması (`-sU`):** TCP protokolünü hiç kullanmaz, doğrudan UDP veri paketleri fırlatır.

#### 2. Güvenlik Duvarı (Firewall) Atlatma Mantığı
* **SYN Taraması (`-sS`):** Basit güvenlik duvarları `SYN` bayrağı taşıyan yeni bağlantı isteklerini hemen fark eder ve engeller.
* **NULL / FIN / Xmas:** Paketlerin içinde `SYN` bayrağı bulunmadığı için, sadece `SYN` paketlerini filtresine almış eski/zayıf güvenlik duvarı kurallarının yanından sıyrılıp içeri sızabilir.
* **UDP:** Port durumunu TCP kurallarıyla tespit edemediği için güvenlik duvarları arkasındaki UDP servislerini bulmak bambaşkadır.
* **Ping Engeli Atlatma (`-Pn`):** Birçok güvenlik duvarı/sistem varsayılan olarak ICMP (ping) paketlerini engeller. Nmap normalde taramaya başlamadan önce hedefin aktif olup olmadığını anlamak için ping atar; yanıt alamazsa taramayı sonlandırır. `-Pn` parametresi, hedefin her halükarda aktif olduğunu kabul ederek ping atmayı atlar ve doğrudan port taramasına geçer.

#### 3. İşletim Sistemi Bağımlılığı ve Doğruluk (Kritik Fark!)
* **Standart Taramalar (`-sT`, `-sS`):** Tüm işletim sistemlerinde (Linux, Windows, macOS, Cisco vb.) %100 standart ve doğru çalışır.
* **NULL / FIN / Xmas:** İşletim sistemine göre davranışı değişir!
    * **Linux / Unix / BSD:** RFC 793 standartlarına uyar. Port kapalıysa `RST` yanıtı verir, açık veya filtreliyse yanıt vermez (`open|filtered`).
    * **Windows / Bazı Cisco Cihazlar:** Standarta uymaz! Port ister açık ister kapalı olsun bu anormal paketlerin hepsine `RST` (Kapalı) yanıtı döner. Bu yüzden Windows hedeflerde bu 3 tarama türü bütün portları yanlışlıkla kapalı gösterir.

---

### 💡 Kısa Özet (Kopya Not)
* **SYN (`-sS`):** Günlük hayatta ve pentest'lerde varsayılan ve en güvenilir tercihtir.
* **NULL/FIN/Xmas:** Sadece "Güvenlik duvarında SYN engeli var mı, arkasına paket geçirebiliyor muyum?" diye test etmek veya Linux hedefli özel sızma durumlarında kullanılır.
* **UDP (`-sU`):** Yalnızca DNS, DHCP, SNMP gibi UDP tabanlı servisleri ararken başvurulan ayrı bir kategoridir.

---

## 🎓 eJPT Sınavı İçin Nmap Kullanım Senaryoları 🎯
eJPT sınavında en sık karşılaşacağınız senaryolar ve bunlara uygun pratik Nmap komut setleri:

### 🌐 Senaryo 1: Ağdaki Aktif Hostları Tespit Etme (Ping Sweep)
Sınav başlangıcında size verilen subnet içerisindeki aktif cihazları hızlıca belirlemek için:
```bash
nmap -sn 192.168.100.0/24
```
> [!TIP]
> Cihazlar ping engelliyse, yerel ağda ARP istekleri ile host keşfi yapıldığından bu komut yerel ağda (local network) son derece hızlı ve doğru çalışır.

### ⚡ Senaryo 2: Hızlı Port Keşfi (TCP Full Port Scan)
Hedef makinedeki tüm (65535) portları hızlıca tarayıp hangilerinin açık olduğunu listelemek için:
```bash
sudo nmap -sS -Pn -T4 -p- <hedef-ip>
```
* **Neden?** Sınavda bazı servisler standart dışı portlarda (örn. 8888 web servisi veya 5555 SSH) çalışıyor olabilir. `-p-` ile tüm portları taramak zafiyeti kaçırmamak için kritiktir.

### 🕵️‍♂️ Senaryo 3: Açık Portlarda Detaylı Servis ve İşletim Sistemi Tespiti
Hızlı taramada açık olduğunu bulduğunuz portlara (örn: 21, 80, 445) özel derin tarama başlatarak zamandan tasarruf edin:
```bash
sudo nmap -sS -sV -sC -O -Pn -p 21,80,445 <hedef-ip>
```
* **Neden?** Sürüm bilgisi (`-sV`) ve varsayılan betikler (`-sC`), hedefteki servislerin zafiyetlerini (örn. eski bir Apache sürümü veya Samba açığı) tespit etmenizi sağlar.

### 🛡️ Senaryo 4: Hedefe Özel Zafiyet Taraması (NSE Exploit & Vuln)
Özellikle Samba (445) veya FTP (21) gibi servislerde kritik zafiyetleri (örn. EternalBlue) veya konfigürasyon hatalarını kontrol etmek için:
```bash
# EternalBlue (MS17-010) kontrolü
sudo nmap -p 445 --script smb-vuln-ms17-010 -Pn <hedef-ip>

# FTP Anonim Giriş kontrolü
sudo nmap -p 21 --script ftp-anon -Pn <hedef-ip>
```

### 🔌 Senaryo 5: SNMP ve UDP Servis Tarama
Sınavda bilgi sızdırmak için kritik olan SNMP (Port 161) veya DNS (Port 53) gibi UDP servislerini taramak için:
```bash
sudo nmap -sU -sV -Pn --top-ports 50 <hedef-ip>
```
> [!IMPORTANT]
> UDP taramaları yavaş sürdüğü için tüm portları taramak yerine `--top-ports 50` veya `-p 53,161` gibi spesifik portları hedefleyin.
